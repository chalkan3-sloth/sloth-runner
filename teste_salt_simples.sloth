-- Teste simples do Salt como objeto - CONFIGURAÇÃO MÍNIMA
print("🧂 Teste Salt Objeto - Configuração Mínima")

-- CONFIGURAÇÃO MÍNIMA - sem master_host desnecessário
local Salt = require("salt")

-- Criar cliente Salt com configuração mínima
local salt_client = Salt({
    timeout = 30,
    retries = 2
})

print("✅ Cliente Salt objeto criado:", type(salt_client))
print("✅ Configuração mínima suficiente!")

-- Testar método do objeto (usando ':')
local result = salt_client:ping("*")
print("📡 Resultado ping:", result.success and "✅ Sucesso" or "❌ Falha")

-- Testar outros métodos
local version = salt_client:version("*")
print("📋 Versão Salt:", version.success and "✅ OK" or "❌ Erro")

print("✨ Salt funcionando sem master_host!")