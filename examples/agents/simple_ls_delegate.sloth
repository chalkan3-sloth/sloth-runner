-- 🚀 EXEMPLO SIMPLES: delegate_to funcionando (LEGACY FORMAT)
-- Este exemplo usa o formato TaskDefinitions que funciona com agentes remotos

TaskDefinitions = {
    -- Grupo de tarefas que roda nos agentes remotos
    remote_ls_demo = {
        description = "Demo LS em agentes remotos usando delegate_to",
        tasks = {
            -- Task 1: LS no ladyguica
            {
                name = "ls_ladyguica",
                description = "Lista arquivos no agente ladyguica",
                command = function()
                    log.info("🔍 Executando LS no ladyguica...")
                    
                    -- Pegar hostname primeiro
                    local hostname_out, _, hostname_failed = exec.run("hostname")
                    local hostname = hostname_out and hostname_out:gsub("\n", "") or "desconhecido"
                    
                    log.info("📍 Host: " .. hostname)
                    
                    -- Executar ls
                    local ls_out, ls_err, ls_failed = exec.run("ls -la $HOME")
                    
                    if not ls_failed then
                        log.info("✅ LS executado com sucesso no " .. hostname)
                        log.info("📂 Arquivos:\n" .. (ls_out or "vazio"))
                        return true, "LS OK no " .. hostname
                    else
                        log.error("❌ LS falhou no " .. hostname)
                        log.error("Erro: " .. (ls_err or "desconhecido"))
                        return false, "LS falhou no " .. hostname
                    end
                end,
                delegate_to = "ladyguica",
                timeout = "30s"
            },
            
            -- Task 2: LS no keiteguica
            {
                name = "ls_keiteguica",
                description = "Lista arquivos no agente keiteguica",
                command = function()
                    log.info("🔍 Executando LS no keiteguica...")
                    
                    -- Pegar hostname primeiro
                    local hostname_out, _, hostname_failed = exec.run("hostname")
                    local hostname = hostname_out and hostname_out:gsub("\n", "") or "desconhecido"
                    
                    log.info("📍 Host: " .. hostname)
                    
                    -- Executar ls
                    local ls_out, ls_err, ls_failed = exec.run("ls -la $HOME")
                    
                    if not ls_failed then
                        log.info("✅ LS executado com sucesso no " .. hostname)
                        log.info("📂 Arquivos:\n" .. (ls_out or "vazio"))
                        return true, "LS OK no " .. hostname
                    else
                        log.error("❌ LS falhou no " .. hostname)
                        log.error("Erro: " .. (ls_err or "desconhecido"))
                        return false, "LS falhou no " .. hostname
                    end
                end,
                delegate_to = "keiteguica",
                timeout = "30s"
            }
        }
    }
}