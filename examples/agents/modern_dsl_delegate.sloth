-- EXEMPLO FUNCIONANDO: Pipeline remota usando Modern DSL
-- Este exemplo executa comandos remotos usando delegate_to

-- Task para execução no ladyguica
local task_ladyguica = task("hostname_on_ladyguica")
    :description("Executa hostname no ladyguica")
    :command(function(this, params)
        log.info("🚀 Executando hostname no ladyguica...")
        
        local output, error, failed = exec.run("hostname && date && echo 'Executado no ladyguica!'")
        
        if not failed then
            log.info("✅ Sucesso no ladyguica!")
            log.info("📝 Saída: " .. output)
            return true, "Hostname executado com sucesso"
        else
            log.error("❌ Falha: " .. (error or "erro desconhecido"))
            return false, "Hostname falhou"
        end
    end)
    :delegate_to("ladyguica")
    :timeout("30s")
    :build()

-- Task para execução no keiteguica
local task_keiteguica = task("ls_on_keiteguica")
    :description("Lista arquivos no keiteguica")
    :command(function(this, params)
        log.info("🔍 Listando arquivos no keiteguica...")
        
        local output, error, failed = exec.run("ls -la $HOME && echo 'Listagem realizada no keiteguica!'")
        
        if not failed then
            log.info("✅ Listagem realizada com sucesso!")
            log.info("📂 Arquivos:\n" .. output)
            return true, "Listagem concluída"
        else
            log.error("❌ Falha na listagem: " .. (error or "erro desconhecido"))
            return false, "Listagem falhou"
        end
    end)
    :delegate_to("keiteguica") 
    :timeout("30s")
    :build()

-- Workflow que coordena a execução
workflow.define("pipeline_remota_completa")
    :description("Pipeline que executa comandos em múltiplos agentes")
    :version("1.0.0")
    :tasks({ task_ladyguica, task_keiteguica })
    :config({
        timeout = "2m"
    })
    :on_complete(function(success, results)
        if success then
            log.info("🎉 PIPELINE REMOTA CONCLUÍDA COM SUCESSO!")
            log.info("✅ Comandos executados nos agentes remotos")
        else
            log.error("❌ Pipeline falhou")
        end
        return true
    end)